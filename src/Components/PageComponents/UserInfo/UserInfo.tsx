import SectionCard from "../../UI/PageElements/SectionCard";

const UserInfo = () => {
  return (
    <SectionCard sectionID="user" title="My Info">
      In this section the user will be able to edit their shit after they are
      logged in. Effect kick in when the save button is clicked. Allow for
      chaning income and location.
    </SectionCard>
  );
};

export default UserInfo;
