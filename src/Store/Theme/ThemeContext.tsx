import React from "react";

export const theme = {
  logoColor: "#5476DD",
  sectionColor: "#F8FAFF",
  pageColor: "#E0E5F1",
  logoFade: [""],
};

export const ThemeContext = React.createContext({
  theme,
});
